{
  "name": "node-react-demo",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@bitbucket.org/spearconcordia/visualizationplatform.git"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "mongoose": "^5.5.11",
    "react-highlight-words": "^0.16.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
